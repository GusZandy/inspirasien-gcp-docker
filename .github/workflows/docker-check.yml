name: Docker Compose CI check
on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  docker-compose-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate docker-compose.yml
        run: docker compose -f docker-compose.yml config --quiet

      - name: Pull images
        run: docker compose -f docker-compose.yml pull

      - name: Ensure external network exists
        run: |
          docker network inspect docker_laravel_network >/dev/null 2>&1 || \
          docker network create docker_laravel_network

      - name: Start services (smoke test)
        run: |
          docker compose -f docker-compose.yml up -d
          docker compose -f docker-compose.yml ps

      - name: Redis healthcheck
        run: docker exec central-redis redis-cli ping | grep -q PONG

      - name: Tear down and remove network
        if: always()
        run: |
          docker compose -f docker-compose.yml down --volumes --remove-orphans
          docker network rm docker_laravel_network >/dev/null 2>&1 || true
